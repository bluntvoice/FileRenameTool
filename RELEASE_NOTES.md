> v2.5 简化文件名清理模式，新增可选日期，并增强对文件名中各种日期形式的识别与更新。

## Highlights

- **只保留文件名更明确**：“仅保留文件名”具有最高优先级，启用后始终只输出文件名和扩展名。
- **日期可单独控制**：普通重命名、清理名称和新建空白 DOCX 均遵循当前日期选项。
- **紧贴日期也能更新**：`文件名-YYYYMMDD修订` 等名称可正确识别旧日期，并替换为当天日期。
- **兼容更多日期形式**：支持连续八位日期、下划线、括号及 `YYYY-MM-DD` 等常见写法。

## 变更

- 移除“仅保留版本号”“仅保留文件名、日期和版本号”等固定清理选项；“仅保留文件名”启用后会禁用并忽略其他命名选项。
- 已有规范版本文件再次处理时继续自动递增版本号，目标版本已存在时自动寻找下一可用版本。
- 日期识别会校验真实日历日期，无效数字不会被误删。

## 修复

- 修复 `文件名-YYYYMMDD修订`、`文件名YYYYMMDD修订` 等名称无法更新日期的问题。
- 修复日期位于括号内或使用 `-`、`_`、`.` 分隔时无法清理的问题。

> [!WARNING]
> 当前 EXE 未使用商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。正式发布后请仅从本仓库 Release 页面下载。

## 下载

| 平台 | 文件 |
|------|------|
| Windows | `FileRenameTool-v2.5.exe` |

该程序为免安装单文件版本，下载后可直接运行。

SHA-256：`A51A8917AB75B369A4176F46FD2D3EC46AE3B891F34CD8187CD7854FE8F4BF33`

**完整变更日志**：https://github.com/bluntvoice/FileRenameTool/compare/v2.4...v2.5
