> v2.3 完善中英文版本命名，新增窗口置顶、规范空白 DOCX 创建及“仅保留文件名、日期和版本号”模式。

## Highlights

- **英文命名更清晰**：公司简称与英文版本类型之间自动加入空格。
- **窗口保持置顶**：通过右上角图钉按钮随时切换置顶状态。
- **直接创建 DOCX**：输入文件名即可按当前命名选项创建 `.docx` 格式的空白文档。
- **新增清理模式**：可只保留文件名、日期、版本号和扩展名。

## 新增

- 新增右上角图钉按钮，使用轻量矢量路径绘制，不增加位图资源模糊。
- 新增“新建空白 DOCX”，无需依赖 Microsoft Office 或第三方组件。
- 新增“仅保留文件名、日期和版本号”，缺少日期或版本号时自动补充。

## 变更

- 公司简称与含英文字母的版本类型组合时自动加入一个空格；中文类型继续保持紧凑连接。
- “仅保留文件名”“仅保留版本号”“仅保留文件名、日期和版本号”采用互斥选择。
- 空白 DOCX 目标名称已存在时，自动递增版本号，避免覆盖原文件。

## 修复

- 新建空白 DOCX 对话框改为自适应宽度，修复高 DPI 缩放下标签与输入框重叠的问题。

> [!WARNING]
> 当前 EXE 未使用商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。正式发布后请仅从本仓库 Release 页面下载。

## 下载

| 平台 | 文件 |
|------|------|
| Windows | `FileRenameTool-v2.3.exe` |

该程序为免安装单文件版本，下载后可直接运行。

SHA-256：`F628241DD3C922E556A05E485082122D0B0637AC563D5C2E8534E3F322ABABDE`

**完整变更日志**：https://github.com/bluntvoice/FileRenameTool/compare/v2.2...v2.3
