> v2.1 在完整交付文件命名、只读处理、单文件快速编辑和界面优化的基础上，进一步提升程序启动速度，让主界面内容更快、一次性呈现。

## Highlights

- **启动更快**：减少启动阶段的正则预编译开销，缩短首次打开等待时间。
- **加载更完整**：合并控件布局、设置读取和预览刷新，避免界面内容分批出现。
- **轻量单文件**：继续保持免安装、无额外运行组件的单 EXE 使用方式。

## 新增

- 支持手动调整文件名部分，并可将选中文件按预览名称另存为新文件。
- 支持版本类型自定义、自动识别、替换和记忆。
- 支持默认解除只读，以及针对无需改名文件的独立“仅解除只读”操作。
- 新增“关于”页面，显示当前版本、作者和 GitHub 仓库地址。

## 变更

- 程序名称统一为 `FileRenameTool`。
- 单文件拖入时自动选中；批量拖入时保留人工确认逻辑。
- 只读文件在列表中显示“（只读）”。
- 软件图标更新。
- README 重新整理为面向使用者的程序介绍、命名规则、使用方法和版本记录。

## 修复

- 修复“仅保留文件名”无法清除 `v1.X`、`v1.2` 等已有版本标记的问题。
- 修复高 DPI 和系统字体缩放下控件遮挡、按钮状态文字对比不足等界面问题。
- 移除文件列表与底部操作区之间的浅蓝色细分隔线。
- 优化启动初始化顺序，减少首次打开时的等待和控件分批显示。
- 修复“关于”窗口首次打开时字体、链接和版本信息加载造成的短暂卡顿。

> [!WARNING]
> 当前 EXE 未使用商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。请仅从本仓库 Release 页面下载。

## 下载

| 平台 | 文件 |
|------|------|
| Windows | `FileRenameTool-v2.1.exe` |

该程序为免安装单文件版本，下载后可直接运行。

SHA-256：`F26E2A929265291AE074E845134FB4CEC13E386DB5F9EB2920E8A0836F914489`

**完整变更日志**：https://github.com/bluntvoice/FileRenameTool/commits/v2.1
