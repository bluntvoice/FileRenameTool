> v2.6 新增可选版本类型，并完善不同扩展名和不完整命名中的日期、版本号识别。

## Highlights

- **版本类型可独立控制**：新增“包含版本类型”选项，可与日期、版本号自由组合；取消后不再写入公司简称及修订版、清洁版等类型。
- **新增英文类型**：默认列表加入 `Revised Version`，位于 `Reviewed Version` 下方。
- **不完整命名也能更新**：`文件名-日期-版本类型` 等缺少版本号的名称会正确识别旧日期并更新。
- **扩展名不干扰规则**：`.docx`、`.pdf`、`.xlsx`、`.pptx` 等后缀及无扩展名文件都使用同一套命名识别逻辑，并保留原后缀。

## 修复

- 修复不完整命名可能未触发日期更新的问题。
- 修复取消版本类型后，旧“版本类型-序号”命名仍保留类型的问题。
- 无效八位数字不再被误判为日期或从文件名中删除。

> [!WARNING]
> 当前 EXE 未使用商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。正式发布后请仅从本仓库 Release 页面下载。

## 下载

| 平台 | 文件 |
|------|------|
| Windows | `FileRenameTool-v2.6.exe` |

该程序为免安装单文件版本，下载后可直接运行。

SHA-256：`F7113CA324EE28ECCA126461D318AE9764446E1200CCD86DF9E5D3A7AD7D40D2`

**完整变更日志**：https://github.com/bluntvoice/FileRenameTool/compare/v2.5...v2.6
